<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.example.imdb">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.hometv.HomeTvActivity">
        </activity>
        <activity android:name=".ui.homemovies.HomeMoviesActivity">
        </activity>
        <activity android:name=".ui.home.HomeAppActivity">
        </activity>
        <activity android:name=".ui.old.moviereview.ReviewActivity">
        </activity>
        <activity android:name=".ui.old.recommendation.RecommendationActivity">
        </activity>
        <activity
                android:name=".ui.old.moviedetail.MovieDetailActivity"
                android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>